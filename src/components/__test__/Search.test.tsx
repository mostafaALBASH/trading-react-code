import React from "react";
import { render, fireEvent, waitForElement } from "@testing-library/react";

import Search from "../Search";

describe("<Search />", () => {
  test("should display search component with respective specs", async () => {
    // assertions
  });
});